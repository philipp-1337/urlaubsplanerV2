rules_version = '2';
// Rules completely open for developing purpose
// TODO: Setup secure and efficient rules
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}